# Working conventions

- When work in this repo is complete, commit and push directly to `main` on GitHub
  (rather than opening a PR or leaving changes on a feature branch). Pushing to `main`
  triggers the `deploy.yml` GitHub Action, which ships to Cloudflare.
